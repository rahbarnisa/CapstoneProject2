# Streamlit Secrets Configuration Example
# Copy this file to .streamlit/secrets.toml (NOT for deployment)
# For deployment, add secrets via Streamlit Cloud UI

OPENAI_API_KEY = "sk-your-api-key-here"

# Note: In production (Streamlit Cloud), add secrets via:
# App Dashboard → Settings → Secrets (not this file)

